# Resumed production — 9 September 2026

Francis authorized resuming through delivery, delegated narrator and music selection, and requested a repository or video download link. This supersedes the earlier requirement to wait for his voice/music choice. No subscription upgrade is inferred.

Editorial music selection: Dreams Become Real by Kevin MacLeod, with the existing CC BY 4.0 attribution. Restrained accompaniment supports the evidence-led script.

Narration preference: a clear female documentary delivery if available within the existing allowance; Adam is the alternate from Francis's list. Provider availability and allowance must be verified before synthesis. Do not call an automated selection a human listening review.

Preserve the September 9 evidence freeze and label its date in the film. Do not silently replace historical counts with current values. Product shots must distinguish captured state from live interactions. Receipt and grant/revoke claims require actual corresponding footage or a script correction.

Delivery must include the hero film, external captions, poster, credits and a QA report that distinguishes mechanical checks from listening and motion review.

## Resumed to delivery — 9 September 2026 (continued)

Picked up after the prior session hit its usage limit mid-QA. State on resume:
`marque-demo.mp4` was already fully assembled (134.6 s, 14 scenes, Sarah
narration, Dreams Become Real bed, silent zero-pass hold). Outstanding:
receipt-scene defect, QA report, the 60-second cut, repository placement.

Changes made:
- **Receipt scene fixed.** The scene had been framing a mid-panel slice of the
  full-page `captures/receipt.png` on a dark frame, so text overlapped and the
  proof grid was unreadable. Recaptured just the four proof blocks
  (`captures/receipt-grid.png`) at a width where the 2×2 grid breathes and the
  charter identifier wraps in its cell, moved the scene to the light frame to
  match the capture, and reframed the crop to show the whole grid. Also
  committed the underlying product fix: `receipt.module.css` now lets a
  `DataCell` wrap inside a receipt row instead of `white-space: nowrap`
  overflowing into the next block at narrow widths.
- **QA.json + SHA256SUMS** generated by `qa.py`. All 13 mechanical checks pass:
  duration within one frame, ≤170 s, 1920×1080/30 fps, bt709/limited/yuv420p,
  stereo 48 kHz, no soft subtitle track, clean full decode, integrated
  loudness -14.3 LUFS, true peak within spec, and the 1.5 s zero-pass hold is
  digitally silent. Human listening and full-motion playback remain unassessed
  and are recorded as such.
- **60-second cut** (`marque-demo-60.mp4`, 61.7 s): question → identity →
  zero → title → bound → mainnet proof → the answer, reusing the per-scene
  clips and the same music treatment including the silent hold. `short.py`.
- **Repository placement.** `demo-video/marque-demo.mp4` (the canonical path)
  now holds this hero cut; `demo-video/marque-demo-60.mp4` and the two SRTs
  sit beside it; the README "For judges" table links the film first. Working
  intermediates (per-scene clips, raw narration, full-page captures) stay
  gitignored; the deliverables, pipeline scripts, frames and evidence are
  tracked.

No narration re-synthesis, no chain action, no deployment. The earlier film
(`demo-video/marque-demo-v2.mp4`, Claude's cut) is left intact on disk.
